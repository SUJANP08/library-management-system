# Copy this file to .env and adjust values before running.
# On Render, set these as Environment Variables in the dashboard instead -
# do NOT commit a real .env file.

# ---------------------------------------------------------------------
# DATABASE
# ---------------------------------------------------------------------
# Local development (default): a SQLite file next to the app.
DATABASE_URL=sqlite:///./library.db

# Production / Render: a managed PostgreSQL instance.
#
# CRITICAL: on Render (or any container host) the filesystem is EPHEMERAL.
# A SQLite file written there is destroyed on every restart, redeploy and
# host migration. This is not related to free-tier sleeping - a paid
# always-on instance still loses the file on redeploy. DATABASE_URL must
# point at a real database server in production.
#
# Paste Render's "Internal Database URL" verbatim; the app rewrites the
# legacy postgres:// scheme automatically, so no hand-editing is needed.
# DATABASE_URL=postgresql://user:password@host:5432/library_db

# ---------------------------------------------------------------------
# AUTH
# ---------------------------------------------------------------------
# Generate a real random secret for production:
#   python -c "import secrets; print(secrets.token_hex(32))"
# Changing this invalidates all existing login tokens (users re-login).
SECRET_KEY=CHANGE_ME_IN_PRODUCTION_super_secret_key

ACCESS_TOKEN_EXPIRE_MINUTES=720

# Only used to create the first admin when the users table is empty.
DEFAULT_ADMIN_USERNAME=admin
DEFAULT_ADMIN_PASSWORD=admin123

# ---------------------------------------------------------------------
# CORS
# ---------------------------------------------------------------------
# Comma-separated list of allowed frontend origins, or * for any.
# Set this to your Netlify URL in production. No trailing slash.
# CORS_ORIGINS=https://your-library.netlify.app,http://localhost:5173
CORS_ORIGINS=*

# ---------------------------------------------------------------------
# FILE STORAGE
# ---------------------------------------------------------------------
# NOTE: these directories are also on the ephemeral filesystem. Backup
# files saved here survive only until the next restart - they are a
# convenience copy, not durable storage. Always download backups via
# GET /api/backup/export and keep them somewhere off the server.
BACKUP_DIR=./backups
UPLOAD_DIR=./uploads
