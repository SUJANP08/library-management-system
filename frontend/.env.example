# Leave unset to use relative /api (works with the bundled nginx proxy or Vite dev proxy).
# Set only if the backend is hosted on a different domain:
# VITE_API_BASE_URL=https://api.yourlibrary.org/api
